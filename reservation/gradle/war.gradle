apply plugin: "war"

bootWar {
    mainClassName = "com.giveitacut.reservation.ReservationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
